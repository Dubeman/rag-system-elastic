"""Vision-native document intelligence pipeline (v2).

Import concrete modules explicitly, e.g. ``from v2.pipeline_v2 import VisionPipelineV2``,
to avoid pulling optional dependencies at package import time.
"""
