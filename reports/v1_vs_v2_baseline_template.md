# v1 vs v2 baseline (template)

Fill after running the same validation questions on both pipelines.

| Metric | v1 (`pipeline_version` v1) | v2 (`pipeline_version` v2) | Notes |
|--------|------------------------------|----------------------------|--------|
| Date / git SHA | | | |
| Query p50 (ms) | | | From logs or `/metrics` |
| Query p95 (ms) | | | |
| Ingest (ms) | | | |
| Retrieval quality | | | e.g. Recall@5, manual |
| Answer quality | | | Short qualitative |

**Validation questions used:**

1.
2.
3.

**Environment:** Docker / bare metal, hardware, model ids (`COLPALI_MODEL_ID`, `VLM_MODEL_NAME`).
